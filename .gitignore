node_modules/
# 密钥
.env
.env.*
openapi_*.json
# 自动备份
.backup/
*.bak
*.bak[0-9]
*.bak[0-9][0-9]
# 实例标识
.instance
# 大文件
*.log
# macOS
.DS_Store
# 运行时数据库（自动生成）
store/heyi.db
store/verify.db
# 运行时状态（每次运行变更）
.det-core-state.json
# 临时工作文件
.acceptance-criteria
.audit-last
.closure-start
.manual-verify
.fact-check-warn
# 运行时状态文件
.deferred-state
.det-explore-baseline.json
.fact-check-heartbeat
.gate-calibration.json
.memory-linkage-state
# git 操作临时（如 rebase 残留）
.git/
